import TaskContainer from "../taskContainer/TaskContainer";
import TaskHeaderContainer from "../taskHeaderCOntainer/TaskHeaderConstainer";

const TeamContainer = (team) => {

    return (
        <div>
            <h1>TeamContainer</h1>
        </div>
    );
}

export default TeamContainer;